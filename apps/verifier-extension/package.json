{
  "name": "verifier-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Dtrust Chrome Extension for zero-interaction verification",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development && node scripts/copy-assets.js",
    "build": "vite build && node scripts/copy-assets.js",
    "preview": "vite preview"
  }
}
